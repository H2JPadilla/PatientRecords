@model EL.PatientEntity
@{
    ViewBag.Title = "Update Prescription";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("UpdatePatient", "Patient", FormMethod.Post, new { id = "patientForm", @class = "needs-validation" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "alert alert-danger" })
    @Html.Partial("Partial/PatientForm", Model)

    <div class="mt-3">
        <button type="button" id="btnSave" class="btn btn-success"
                data-modal-title="Confirm Update"
                data-modal-body="Are you sure you want to update this patient record?"
                data-modal-confirm-text="Yes, Update"
                data-modal-confirm-class="btn-warning">
            Save
        </button>
        @Html.ActionLink("Cancel", "ViewPatient", "Patient", new { area = "Admin" }, new { @class = "btn btn-secondary" })
    </div>
}
